// to make cursor as pointer
$('#brand').css("cursor", "pointer");

// to go back to home page (index.html) if we are any other page
$('#brand').click(function(){
    window.location.href="../index.html";
});